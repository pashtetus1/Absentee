{
  "name": "absentee",
  "version": "0.0.0",
  "private": true,
  "description": "Absentee — прототип игры про космическую империю, которой нельзя управлять напрямую",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "check": "tsc --noEmit",
    "test": "tsc --noEmit && node build.mjs && node test/run.ts",
    "compare": "node test/compare.ts"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "esbuild": "^0.28.2",
    "typescript": "^5.9.3"
  },
  "type": "module"
}
