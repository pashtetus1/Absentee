{
  "name": "porog",
  "version": "0.0.0",
  "private": true,
  "description": "Порог — прототип игры про космическую империю, которой нельзя управлять напрямую",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "check": "tsc --noEmit",
    "test": "node build.mjs && node test/run.js",
    "compare": "node test/compare.js"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "typescript": "^5.9.3"
  }
}
