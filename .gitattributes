# Переводы строк держим LF везде. Иначе git разворачивает файлы в CRLF, правки
# скриптами пишут LF, и в одном файле оказываются оба вида: инструменты,
# разбирающие исходник построчно, начинают спотыкаться на невидимом \r.
* text=auto eol=lf
